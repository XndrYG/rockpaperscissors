extends layout

block content
    div.card-container
        div.card Player Choice
            img(src=`/images/${playerChoice}.jpg`)

        p VS
        div.card PC Choice
            img(src=`/images/${pcChoice}.jpg`)


    h3 Winner: #{winner}

    a(href='/') Play Again